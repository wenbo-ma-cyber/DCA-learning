# Python 教学项目验证

核查日期：2026-09-21。实际环境 Python 3.12，pandas 2.2.3、NumPy 2.5.3、Matplotlib 3.11.2。依赖仅安装到交付目录内的临时测试目录，验证后移除。

- `timeout 60 ... backtest.py --self-test`：通过。覆盖零收益、持续下跌、入金金额放大但净值不变、实际日期 XIRR、无效持有期限。
- `timeout 60 ... backtest.py`：通过，产出四组账本、summary.csv、comparison.png。已人工检查图表标签、完整显示和曲线。
- 所有组累计投入48000。模拟期末金额：A100 57789.331948；A80_B20 56257.671243；A60_B40 54726.010537；B100 50131.028422。
- 20课数据结构及全部Python代码片段语法检查通过；随后使用 check-lessons.py 在同一命名空间按 Day41–60 顺序实际执行，20段全部成功。CSV 注释通过 comment="#" 正确处理。片段按课程顺序共享变量，不是每段独立的完整程序。

这些验证只证明列出的模拟与数学情景，未验证真实券商成交、市场行情或收益预测能力。示例图与CSV保留在results中便于对照；可重新运行脚本生成。

## 输入边界复验

simulate 现在显式拒绝空/单行、重复日期、非升序日期、NaT、非日期索引、非数值/非有限/非正价格，以及实际跨度不足一天的输入。短样本仅一个有效收益时，annual_volatility_252 为 NaN，volatility_status 为“不可计算：至少需要两个有效日收益观测”。

扩充后的 --self-test、完整四组回测和20课片段执行均在独立60秒超时下通过（均不足1秒）。四组统一为 A100、A80_B20、A60_B40、B100，原70/30与50/50结果已移除。20课实际执行未发现新的运行错误；验证过程中生成的 normalized.png 保留为 Day45 示例输出。
