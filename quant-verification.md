# 量化学习模块验证记录

验证日期：2026-09-26。基于公开仓库 main 的 `2e72c6455d25f06540705f4f8460b5ec5811d657`。工作副本独立于本地早期 `dca-learning/` 目录，未覆盖其中未提交内容。

## 自动检查

- `timeout 60s node tests/quant-core.test.cjs`：12 组通过。涵盖同类筛选与排序、动量窗口、非法输入、逆波动、资金守恒、零资金、阈值边界及接口导出。
- `timeout 60s node tests/quant-integration.test.cjs`：通过。60 + 8 个唯一课程 ID；旧 v1 备份兼容；量化进度、笔记与答案往返校验；非法记录拒绝；课程结构；8 段 Python 标准库代码实际执行；本地页面资源存在与脚本顺序。
- `node --check app.js`、`node --check quant.js`、`node --check quant-curriculum.js`：通过。
- `git diff --check`：通过。

## 浏览器检查

通过 Codex 浏览器控制接口操作本地页面 `http://127.0.0.1:8766/dca-learning-quant/`，含仓库子路径。

| 检查 | 结果 |
| --- | --- |
| 量化总览、8 节课程、每课 2 道测验、4 个嵌入实验 | 全部可进入并显示正确默认结果 |
| 基础课程笔记与完成状态刷新 | 保留 |
| 量化笔记、答案、打卡刷新 | 保留；基础课程仍只计 1/60，量化独立计 1/8 |
| 我的笔记汇总 | 同时展示基础和量化笔记，链接回对应课程 |
| 笔记中 `<b>` 文本 | 作为原文显示，不解释为 HTML |
| 基金筛选默认 | A、B、F 共 3/6 个；费用为 0 时得到空结果；重置恢复 3/6 |
| 动量默认 | 6 个月跳过最新月，区间 M6→M12，B 排第一 |
| 动量不跳过最新月 | 区间 M7→M13，C 排第一，B 为 −17.29% |
| 逆波动默认 20/10/5 | 权重 14.29% / 28.57% / 57.14% |
| 逆波动将 C 改为 20 | 权重 25% / 50% / 25% |
| 新增资金默认 | 8000/2000 持仓，投入 1000 全部补债券，投入后 72.73%/27.27% |
| 独立买卖再平衡默认 | 股票卖出 2000、债券买入 2000，恢复 60%/40% |
| 零持仓、零新增资金 | 全部金额和权重为零，不触发交易，无 NaN/Infinity |
| 无效备份 | 含 `quant-99` 被拒绝，当前数据不改动 |
| 旧备份恢复 | 基础 1/60、量化 0/8；正常恢复 |
| 含量化记录的新备份恢复 | 基础 1/60、量化 1/8；2 篇笔记、1 课答案被接受 |
| 原有投资实验室 | 复利、定投、最大回撤均正常；原例定投期末 5250 元、回撤 −40% |
| 桌面、390 px 与 320 px | 实际截图检查；页面没有整体横向溢出，宽表格在容器内横向滚动 |
| 最终新开预览页 | 浏览器控制台 error / warn 记录为空 |

发现并修正：波动率输入的 HTML 步长与最小值不匹配；320 px 下时间单位孤行。原入口引用的 5 个缺失脚本/样式已移除，保留仓库已有的实际功能。

测试数据仅写入本次独立本地预览的存储，完成后通过空测试备份恢复。未操作正式站点的学习记录。

## 范围与限制

- 实验均为教学模拟，没有真实行情、自动交易或收益预测；未执行真实基金策略回测。
- 未重新运行原有下载目录的 Python 定投项目，也没有构建步骤。
- 本次未推送 GitHub、创建 PR 或触发 GitHub Pages 发布。
- 旧网站版本不认识 `quant-*` 课程 ID；部署需同时更新入口、应用与四个量化文件。
